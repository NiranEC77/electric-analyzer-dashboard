# dependencies / build output
node_modules/
dist/
build/
.astro/
.vercel/
*.tsbuildinfo
coverage/

# secrets — never committed, see .env.example for the documented shape
.env
.env.*
!.env.example

# PII guardrails — this repo is the system, never the data.
# Real bills, parsed facts, and account-identifying material must never be committed.
*.pdf
/data/**
**/user-data/**

# OS / editor
.DS_Store
*.log

# local tooling state (not part of the project)
.ijfw/
